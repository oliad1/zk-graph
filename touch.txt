PLAN:
- Mouse interactions
  - Fix edges highlight
- Switch notify-debouncer to notify for background loading
- Use same egui window context for all selected nodes
- Add max and min scrolling with egui
- Smoother scrolling
- Custom node display
  - Nodes vary in size (clamp min) based on # of links
  - Label appears beneath node
  - Label moves slightly more down after node is selected
  - Label colour is unaffected by node color
  - If I scroll in to a certain threshold, all labels show
- Fix laggy egui::Window vertical scrolling
- Read zk from cli arg or zk config or asssume we're in the right root
